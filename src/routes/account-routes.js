import express from 'express';

const router = express.Router();

router.route('/logon')
  .post((req, res) => {
  
  });

router.route('/logoff')
  .post((req, res) => {
  
  });

module.exports = router;
